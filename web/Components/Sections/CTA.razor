<section class="w-full bg-primary py-16 text-white">
    <div class="mx-auto max-w-[960px] px-4 text-center">
        <span class="material-symbols-outlined mb-4 text-5xl opacity-90">
            favorite
        </span>

        <h2 class="font-black mb-6 text-3xl md:text-4xl">
            Sua ajuda é a nossa força
        </h2>

        <p class="mx-auto mb-10 max-w-2xl text-lg font-medium opacity-90 md:text-xl">
            Não importa o valor, cada doação contribui diretamente para manter
            nossos projetos vivos e levar esperança para quem mais precisa.
        </p>

        @*
        <div class="flex flex-col justify-center gap-4 sm:flex-row">
            <button
                class="rounded-lg bg-white px-8 py-3 text-lg font-bold text-primary
                       shadow-lg transition-colors hover:bg-gray-100"
                @onclick="HandleDonateClick">
                Quero Doar Agora
            </button>

            <button
                class="rounded-lg border-2 border-white px-8 py-3 text-lg font-bold
                       text-white transition-colors hover:bg-white/10"
                @onclick="HandleVolunteerClick">
                Seja um Voluntário
            </button>
        </div>
        *@
    </div>
</section>
